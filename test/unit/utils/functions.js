import expect from 'expect';

import { sortArray } from '../../../src/js/utils/functions';

describe('calendarDateCheck function', () => {

	it('should have a function export called base', () => {
		expect(
			sortArray
		).toExist();
	});

});